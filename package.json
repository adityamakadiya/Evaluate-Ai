{
  "name": "evaluateai-v2",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "setup": "pnpm install && pnpm run build && cd packages/cli && npm link && cd ../.. && echo '✓ evalai installed globally. Run: evalai init'"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.30.3",
  "dependencies": {
    "pg": "^8.20.0"
  }
}
